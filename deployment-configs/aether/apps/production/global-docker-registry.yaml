secrets:
  htpasswd: ""
persistence:
  enabled: false
ingress:
  enabled: true
  hosts:
    - "registry.central.aetherproject.net"
  annotations:
     "nginx.ingress.kubernetes.io/proxy-body-size": "0"
